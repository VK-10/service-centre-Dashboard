{{template "top" .}}
    <title>Vehicle Status</title>
</head>
<body class="p-8">
  <div class="max-w-md mx-auto text-center">
    <h1 class="text-2xl font-bold mb-4">Vehicle-Service Placed Successfully!</h1> 
    <p class="text-lg mb-2">Vehicle-Service ID:</p>
    <p class="text-xl font-bold mb-4">{{.Vehicle.ID}}</p>
  </div>
{{template "bottom" .}}